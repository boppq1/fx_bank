<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bank.fx.dao.FxDataDao">

    <sql id="rateColumns">
        fx_rate_no, currency_code, buy_rate, sell_rate, base_rate, announced_at
    </sql>

    <insert id="insertRate">
        INSERT INTO exchange_rates (
            fx_rate_no, currency_code, buy_rate, sell_rate, base_rate, announced_at
        ) VALUES (
            SEQ_EXCHANGE_RATES.NEXTVAL,
            #{currency_code},
            #{buy_rate},
            #{sell_rate},
            #{base_rate},
            TO_TIMESTAMP(#{announced_at}, 'YYYYMMDDHH24MISS')
        )
    </insert>

    <select id="countRatesByDate" resultType="int">
        SELECT COUNT(*)
        FROM exchange_rates
        WHERE TRUNC(announced_at) = TO_DATE(#{announced_date}, 'YYYYMMDD')
    </select>

    <select id="selectLatestRatesByCurrencies" resultType="com.example.bank.fx.dto.FxDataDto">
        SELECT <include refid="rateColumns"/>
        FROM (
            SELECT er.*,
                   ROW_NUMBER() OVER (PARTITION BY currency_code ORDER BY announced_at DESC) rn
            FROM exchange_rates er
            WHERE currency_code IN
            <foreach collection="currencyCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        )
        WHERE rn = 1
    </select>

    <select id="selectAllLatestRates" resultType="com.example.bank.fx.dto.FxDataDto">
        SELECT <include refid="rateColumns"/>
        FROM (
            SELECT er.*,
                   ROW_NUMBER() OVER (PARTITION BY currency_code ORDER BY announced_at DESC) rn
            FROM exchange_rates er
        )
        WHERE rn = 1
        ORDER BY currency_code
    </select>

    <select id="selectAll" resultType="com.example.bank.fx.dto.FxDataDto">
        SELECT <include refid="rateColumns"/>
        FROM exchange_rates
        ORDER BY announced_at DESC, currency_code
    </select>

    <select id="selectRateHistory" resultType="com.example.bank.fx.dto.FxDataDto">
        SELECT <include refid="rateColumns"/>
        FROM exchange_rates
        WHERE currency_code = #{currencyCode}
        ORDER BY announced_at DESC
    </select>

    <select id="selectLatestRate" resultType="com.example.bank.fx.dto.FxDataDto">
        SELECT <include refid="rateColumns"/>
        FROM exchange_rates
        WHERE currency_code = #{currencyCode}
          AND announced_at = (
              SELECT MAX(announced_at)
              FROM exchange_rates
              WHERE currency_code = #{currencyCode}
                AND TRUNC(announced_at) &lt;= TO_DATE(#{date}, 'YYYY-MM-DD')
          )
    </select>

</mapper>
